# AGENTS.md — jev-mcp 開発ガイド

このリポジトリの Issue（#1〜#25）を 1 Issue = 1 PR で実装するエージェントと開発者向けの行動規範。
仕様の本体は設計書 `docs/00`〜`docs/06`。このファイルはその運用ルールであり、設計書に矛盾する
記述があれば設計書を正とする（このファイル側を直す）。

## 最重要原則（5 つ — 違反は設計違反として扱う）

1. **決定的に済む部分は機械で。意味の判断だけ Jev に委ねる**。構造検証・パーサ・PBT 生成・
   変異テスト・依存グラフは決定的に書く。境界は人間へのエスカレーションが正しい挙動
   （手抜きではない）
2. **判定の実行と決定は jev-core 内で完結する**。ホスト（Claude Code / goose）には
   アクション（pass / block / warn / escalate）のみを返し、確率 p は決して渡さない。
   p をツール出力・recipe 指示文・PR コメントに載せない
3. **確率の二値化は回答層の 1 か所にのみ集約する**。`decision` は回答層の
   true / false / unknown だけを使って書く。設計書の「p > 0.6 で警告」等の記述は
   しきい値上書きの略記として実装する（docs/05）
4. **observe してから信じる**。observe は `gate: "reversible"` の block 型に限定。
   closed ゲート（機密・不可逆操作の前）の本番 observe は禁止 —— 合成ゴールデン +
   ステージングで検証する
5. **unknown を false に潰さない**。confidence 欠落は常に unknown。

## Jev 判定の実装ルール（jev-claude の罠リスト継承）

判定コードを書くときは次を守る。詳細は docs/05:

- 正規化: `p_norm = score / (criteria.length - 1)`（criteria 1 件のときは 1）
- 確定規則（初期値。判定ポイントが上書きできる）:
  - `true`: p ≥ 0.75 かつ confidence ≥ 0.5
  - `false`: p ≤ 0.25 かつ confidence ≥ 0.5
  - `unknown`: 上記以外（中間帯、confidence < 0.5、confidence 欠落）
- 1 criterion 1 質問。複数の判断を 1 criterion に混ぜない
- evidence は `meta`（組立てた事実列。判定語を含めない）と `data`（対象データを**生のまま**、
  加工・要約・評価語なし）の 2 セクション + 境界マーカー + docs/05 の固定文で組立てる
- `block` の reason は事実のみ。判定語（「違反」「迂回した」等の断定）を書かない
- `judge` は失敗時に例外を投げず `status: "failed"` を返す。呼び出し側は `failMode` に従う。
  `failMode` の指定を省略しない
- fail-open が既定。`closed` は不可逆操作の前の判定のみに限る（判定器の常時故障で
  フロー全体が止まる形を作らない）
- 総予算は `budgetMs` + AbortSignal で自前管理する（SDK のタイムアウトは 1 試行あたり）
- 多数決: 同一 evidence で 3 回判定、p の幅 ≥ 0.3 なら unknown
- 各判定ポイントのゴールデンに注入ケース（evidence 内の指示文）を 1 件以上必須にする

## MCP サーバーのルール

- `judge` ツール契約は docs/06 どおり。出力に p・confidence・answers を含めない
- evidence は `kind: "paths"` が基本。inline は小さいもののみ（転写ミスと機密漏出の防止）
- 判定ポイント定義（質問・しきい値・decision）はホストごとに変えない。定義は 1 か所。
  「goose でうまく動かないから」としきい値を変えない。変えるなら jev-core 側で変え、
  両ホストで同じゴールデンを回す
- jev-judge / thought-graph に触る変更は、**Claude Code と goose の両方**で同一 point・
  同一 evidence → 同一 action を確認してから PR を出す（README ゴール 1）

## アダプタ層のルール

- アダプタは「ホストの生ログ → 共通イベントスキーマ（docs/02）」の**決定的な変換のみ**。
  判断・分類・解釈をアダプタに書かない
- 取れないイベントは「取れない」として欠落のまま流す（解釈して埋めない）
- アダプタのテストはホストの実出力をフィクスチャ化した回帰で行う（フック仕様は版で変わる）

## Issue → PR プロセス

- **1 Issue = 1 PR**。依存 Issue が open のままなら着手しない
- 着手順: Phase 0（#1〜#6）→ Phase 1（#7〜#11）→ Phase 2（#12〜#16）→ Phase 3（#17〜#21）→
  Phase 4（#22〜#25）。**判定を使うステップは Phase 0 完了まで着手しない**。判定を使わない
  決定的部分（#7 の DSL・#17 の層 1・#22 の L0・収集/正規化など）は Phase 0 と並行で
  着手してよい（docs/05）
- ブランチ: `issue/<番号>-<slug>`（例: `issue/1-jev-core-judge`）
- 着手時の手順:
  1. Issue 本文と参照節（docs）を読む
  2. **DoD（完了の定義）をテストとして先に書く**（DoD の各項目が通るテスト・手順を用意）
  3. 実装する
  4. **PR 出す前の必須ゲート（下記 2 つ）を通す**
  5. DoD を 1 項目ずつ確認する
  6. PR を出す
- PR 本文には DoD チェックリストを転写し、`[x]` と証跡（テスト名・実行結果・確認手順）を添える
- 設計書の更新を含む Issue（しきい値の最終値確定など）は、**設計書の差分も同一 PR に含める**
- コミットメッセージ: 英語・命令形・1 行要約。PR はマージ後に Issue を close
- **誠実さのルール**: DoD に「人間レビュー 20 件」「observe 記録 20 件」のような
  人間の作業がある場合、エージェントはそれを達成したことにしない。記録を積むための
  実装とコマンドを提供し、残った人間タスクを PR に明記して Issue は open のままにする

## PR 出す前の必須ゲート（未実施の PR は出さない）

実装した本人が「動くはず」で終わらせない。次の 2 つを通すまで PR を出さない。

### 1. ローカル動作確認

- テストスイートを全実行して緑を確認する（`.skip`・TODO を残したままにしない）
- 触った判定ポイントのゴールデン回帰を実行する
- **DoD の動作条件を実際にローカルで動かして確認する**。テストで証明できないもの
  （フック配線、MCP サーバー登録、recipe 完走、CLI の出力）は必ず手動で動かす —
  「ビルドが通る」「ユニットテストが緑」は動作確認ではない
- PR には実行したコマンドと結果の証跡を添える

### 2. 専用エージェントによるセルフレビュー

- 実装完了後、**実装とは別コンテキストのレビュー専用エージェント**に diff をレビューさせる
  （Claude Code ではサブエージェント、goose では別セッション）。実装者自身の context に
  引きずられないよう、渡すのは diff・Issue 本文・参照節（docs）のみ
- レビュー観点を指定する: 設計書（docs/05・06）との整合、最重要原則の遵守、
  テストの妥当性、エッジケース、機密の扱い
- 指摘は鵜呑みにしない。1 件ずつ独立に評価し、正当な問題は修正してから PR を出す。
  採用しない指摘には理由を付ける
- PR 本文に「レビュー指摘への対応」を書く（指摘 → 採用 / 却下と理由 → 対応内容）

## テスト方針

- 決定的ロジック（正規化・パーサ・集約・アダプタ変換）はネットワークなしのユニットテスト
- **Jev を呼ぶテストで実 API を叩かない**。合成 evidence・ゴールデン・記録応答を使う
- テスト由来のログに `golden-` / `mj-` 接頭辞を付け、レポートで分けて数える
- ゴールデンの expected は**人手で確定**する。テストが expected を自動生成して
  上書きしない。境界で block/pass が揺れる同一入力は FLAKY リストに入れて分母から外す
- ゴールデン回帰を壊す変更は PR で理由を書く

## 技術スタック・リポジトリ規約

- Node.js（LTS）+ TypeScript（ESM）。jev-claude からの移植部（jev-lib.mjs、transcript.mjs）は
  JSDoc 付きで維持し、core から呼んでよい
- テスト: vitest
- 依存最小主義: 標準ライブラリ優先。依存の追加は PR で理由を書く
- ディレクトリ: `packages/core` / `thought-graph` / `dual` / `adapters`（claude-code / goose）。
  アダプタ固有の知識が core に漏れたら設計違反
- 判定ログ・スナップショット・ゴールデン実データはリポジトリに入れない
  （0700 / 0600、機密を含むため）

## 迷ったときの優先順

1. このファイルの最重要原則（特に p を渡さない・unknown を潰さない・observe の限定）
2. docs/05（判定の型・規則）→ docs/06（ホスト契約）
3. 各設計書の該当節 → Issue の DoD
4. それでも不明なら、実装を決めずに PR に「未解決の質問」として書く。
   原則を曲げる方向での解決をしない（p を出力に入れる、unknown を false に潰す、
   closed を observe する、は絶対にしない）

## Claude Code / goose 固有

- **Claude Code**: `CLAUDE.md` がこのファイルを読み込む。hooks の設定は `.claude/` 配下、
  収集は PostToolUse、完了判定は Stop フック
- **goose**: このファイル（AGENTS.md）を自動で読む。recipe は `packages/dual`、
  extensions 登録は stdio MCP サーバーとして行う
- goose フックの正確な仕様（イベント種別・ペイロード）は設計時点で一部未確認（docs/06 の注）。
  実装時に公式ドキュメントで確定し、docs/06 を更新する（#6 の DoD）
